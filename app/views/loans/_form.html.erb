<%= simple_form_for([@book, @book.loans.build]) do |f| %>
  <div class="form-inputs">
    <%= f.input :lender %>
    <%= f.input :returns %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
